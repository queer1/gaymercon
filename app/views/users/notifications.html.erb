<h3>Activity</h3>
<%= render :partial => "notification", :collection => @notifications %>
<% if @notifications.empty? %>
  <h3>Nothing here yet! <%= link_to "Join some groups", groups_path %> to start receiving notifications!</h3>
<% end %>